import { ComingSoon } from "@/components/empty-state";

export default function StubPage() {
  return <ComingSoon title="Login activity" description="Recent login history with device, IP, location, anomaly flags." href="/admin" />;
}
