import { ComingSoon } from "@/components/empty-state";

export default function StubPage() {
  return <ComingSoon title="Email channels" description="Inbound email addresses, forwarding, signature stripping, auto-response." href="/admin" />;
}
