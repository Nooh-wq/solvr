import { ComingSoon } from "@/components/empty-state";

export default function StubPage() {
  return <ComingSoon title="Voice" description="Twilio / Amazon Connect configuration (flagged as future — M16 gate)." href="/admin" />;
}
